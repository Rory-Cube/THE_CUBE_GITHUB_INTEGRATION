views:
  - name: messages_view

    cubes:
      - join_path: messages
        includes: "*"

      - join_path: messages.messages_users
        prefix: true
        includes: "*"

      - join_path: messages.messages_recipients
        prefix: true
        includes: "*"

    # access_policy:
    #   - group: "rory"
        # row_level:
        #   filters:
        #     - member: id
        #       operator: equals
        #       values: [ "{ userAttributes.customer_id }" ]
